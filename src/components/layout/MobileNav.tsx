// Replaced by the persistent left sidebar in AppShell.tsx. Kept as a
// no-op so the ~30 pages that still render <MobileNav /> don't need
// individual edits.
export function MobileNav() {
  return null;
}
